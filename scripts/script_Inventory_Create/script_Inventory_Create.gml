///===Inventory Create===///
//Use to create inventory on character

//creates new inventory object and stores instance ID
inventory_Instance_ID = instance_create_layer(0, 0, "inventory_Objects", obj_Inventory);